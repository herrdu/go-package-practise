package main

import (
	"fmt"
	"strings"
)

func main() {
	custom()
}

func custom() {
	const input = "1234 5678 12345678901234567890"
	sr := strings.NewReader(input)

	fmt.Println(sr.Read
	fmt.Println(sr.Len())
	fmt.Println(sr.Len())
	fmt.Println(sr.Len())
	fmt.Println(sr.Len())
	fmt.Println(sr.Len())
	fmt.Println(sr.Len())
	fmt.Println(sr)

	// scanner := bufio.NewScanner(sr)

}
